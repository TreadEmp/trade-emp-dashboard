import React from "react";
// import ReactPlayer from "react-player";

export default function VideoPlayer({ url }) {
  return (
    <div style={{ marginTop: "20px" }}>{/* <ReactPlayer url={url} /> */}</div>
  );
}
